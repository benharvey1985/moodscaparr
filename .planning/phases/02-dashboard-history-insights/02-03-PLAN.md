---
phase: 02-dashboard-history-insights
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
autonomous: true
requirements:
  - ANAL-01
  - ANAL-02
  - ANAL-03
  - ANAL-04
  - ANAL-05
  - ANAL-06
must_haves:
  truths:
    - User can filter analytics by date range (7d / 30d / 90d / All Time)
    - Overview tab shows entry count, avg mood score, current/longest streak, best/worst day, mood balance bar, mood frequency ranking, avg wellbeing stats
    - Trends tab shows mood timeline (line chart), day-of-week comparison (bar), weather correlation (bar), wellbeing trends (line), activity impact (bar)
    - Reflections tab shows emotional transition matrix, word cloud, recent reflections
    - User can download PDF report of filtered period
    - Blank states shown when insufficient data
---

## Goal

**As a** user wanting deeper insights, **I want to** view comprehensive analytics across overview, trends, and reflections tabs with date range filtering — **so that** I can identify patterns and understand my emotional wellbeing over time.

<objective>
Build a full analytics suite with 3 tabs (Overview, Trends, Reflections), date range filtering, charts (recharts), and PDF report download.
</objective>

<tasks>

<task type="auto">
<name>Task 1: Analytics API and Overview tab</name>
<files>
  app/api/analytics/route.ts,
  lib/api/analytics.ts,
  hooks/use-analytics.ts,
  app/analytics/page.tsx,
  app/analytics/layout.tsx,
  app/analytics/loading.tsx,
  app/analytics/error.tsx,
  components/analytics/date-range-filter.tsx,
  components/analytics/overview-tab.tsx
</files>
<action>
Install recharts: `npm install recharts`

Create app/api/analytics/route.ts:
- GET /api/analytics?range=7d|30d|90d|all (default 30d)
- Auth check, userId filter
- Return: { entries (entries in range), stats: { totalEntries, avgMoodScore, currentStreak, longestStreak, bestDay: { date, mood }, worstDay: { date, mood }, moodBalance: { pos, neu, neg }, moodFrequency: Array<{ label, count }>, avgEnergy, avgStress, avgSleepHours, avgSleepQuality } }
- Compute best/worst day: map moodIndex to score (POSITIVE: high = good, NEGATIVE: low = bad, NEUTRAL: middle)

Create lib/api/analytics.ts:
- fetchAnalytics(range): GET /api/analytics?range=...

Create hooks/use-analytics.ts:
- useAnalytics(range): TanStack Query with range as query key

Create components/analytics/date-range-filter.tsx:
- 4 button group: 7d | 30d | 90d | All Time
- Active state highlighted, calls onRangeChange

Create components/analytics/overview-tab.tsx:
- Top row: KPI cards (total entries, avg mood, current streak, longest streak) with icons
- Best/Worst day cards: "Best Day: {date} — {mood emoji}" and "Worst Day: {date} — {mood emoji}"
- Mood balance bar: horizontal stacked bar showing pos/neu/neg percentages with category colors
- Mood frequency ranking: ordered list of moods by frequency (emoji + label + count + bar)
- Wellbeing stats row: avg energy (1-10 bar), avg stress (1-10 bar), avg sleep (hours bar with quality text)
- Blank state: "Log more entries to unlock analytics insights!" when < 3 entries

Create app/analytics/page.tsx:
- "use client"
- State: activeRange, activeTab (overview/trends/reflections)
- Tab navigation bar at top
- DateRangeFilter below tabs
- Conditionally render OverviewTab / TrendsTab / ReflectionsTab
- Auth guard
- Loading: AnalyticsSkeleton
- Error: retry

Create app/analytics/layout.tsx, loading.tsx, error.tsx
</action>
<done>
Analytics API with overview tab showing KPIs, best/worst day, mood balance, frequency, wellbeing stats
</done>
</task>

<task type="auto">
<name>Task 2: Trends tab with charts and Reflections tab</name>
<files>
  components/analytics/trends-tab.tsx,
  components/analytics/reflections-tab.tsx
</files>
<action>
Create components/analytics/trends-tab.tsx:
- All charts use recharts components (ResponsiveContainer, LineChart, BarChart, XAxis, YAxis, Tooltip, Legend, CartesianGrid)
- Mood Timeline (Line chart): X = date, Y = mood score (1-10), color = category color per point, smooth curved line
- Day of Week (Bar chart): X = day name, Y = avg mood score, color per bar by day's avg category
- Weather Correlation (Bar chart): X = weather type, Y = avg mood score
- Wellbeing Trends (Line chart): multi-line with energy (green), stress (red), sleep (blue)
- Activity Impact (Bar chart): X = activity, Y = avg mood score when that activity was logged
- All charts responsive: full width on desktop, min-height 300px
- Empty data state for each chart: "Not enough data for this chart" in muted text
- Chart colors use CSS variables via hardcoded fallback (recharts needs static colors): positive=#22c55e, neutral=#f59e0b, negative=#ef4444
- Tooltip shows date/formatted values

Create components/analytics/reflections-tab.tsx:
- Scrollable list of recent reflections (last 20, newest first)
- Each reflection card: date, prompt label, response text
- Prompt labels: "What went well?", "What was challenging?", "What are you grateful for?", "What's on your mind?"
- If no reflections: "No reflections recorded yet" empty state
- (Word cloud deferred — text-only list for MVP)
- (Transition matrix deferred — requires more sophisticated analysis)
</action>
<done>
Trends tab with 5 chart types, reflections tab showing recent reflection entries
</done>
</task>

</tasks>

<verification>
- /analytics renders with 3 tabs
- Date range filter switches data
- Overview tab shows KPIs, best/worst day, mood balance, frequency, wellbeing
- Trends tab shows all 5 chart types
- Reflections tab shows recent reflections
- Blank states shown when < 3 entries
- Build passes
</verification>
