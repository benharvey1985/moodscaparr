---
phase: 02-dashboard-history-insights
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - HIST-05
  - HIST-06
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05
  - CAL-06
  - CAL-07
must_haves:
  truths:
    - Entry history page shows full list sorted by date (newest first)
    - User can search entries by text across mood, reflections, activities, weather
    - User can filter by mood category (Positive/Neutral/Negative)
    - User can click entry to view full detail in a dialog
    - User can edit and delete from history view
    - User can export entries as CSV
    - Calendar heatmap shows monthly grid with color-coded cells
    - User can navigate months (prev/next)
    - Month summary stats shown
    - Click day shows entry detail
    - Today is highlighted
    - Color legend displayed
    - Default view starts from user's first entry month
---

## Goal

**As a** user exploring my mood patterns, **I want to** browse past entries with search and filter, view them on a calendar heatmap, and export my data — **so that** I can understand my emotional trends and keep a personal record.

<objective>
Build the full entry history page (search, filter, pagination, detail dialog, edit/delete, CSV export) and a calendar heatmap (monthly grid, color-coded, navigation, stats, detail on click).
</objective>

<tasks>

<task type="auto">
<name>Task 1: Entry history page with search, filter, detail, and CSV export</name>
<files>
  app/history/page.tsx,
  app/history/layout.tsx,
  app/history/loading.tsx,
  app/history/error.tsx,
  components/history/entry-list.tsx,
  components/history/search-bar.tsx,
  components/history/filter-bar.tsx,
  components/history/entry-detail-dialog.tsx,
  components/history/csv-export.tsx,
  app/api/mood-entries/search/route.ts,
  hooks/use-history.ts,
  lib/api/history.ts
</files>
<action>
Create components/history/search-bar.tsx:
- Text input with search icon (lucide Search)
- Debounced (300ms) input, calls onSearch callback
- Clear button when text is present

Create components/history/filter-bar.tsx:
- 3 toggle buttons: All | 😊 Positive | 🧘 Neutral | 😢 Negative
- Active state colored with category CSS variable

Create components/history/entry-detail-dialog.tsx:
- shadcn/ui Dialog
- Shows full entry: mood emoji (2rem), label, category color strip, date formatted (date-fns), intensity with visual bar
- Context section: activities chips, weather, sleep (hours + quality), energy level bar, stress level bar
- Reflections section: 4 prompts with responses (or "Not answered" in muted text)
- Edit button → /wizard?id=ENTRY_ID
- Delete button → DeleteDialog component
- Scrollable content for long reflections

Create components/history/csv-export.tsx:
- Button with Download icon
- On click: fetch all entries for user, format as CSV:
  - Headers: Date, Category, Mood, Intensity, Activities, Weather, Sleep Hours, Sleep Quality, Energy, Stress, Reflection 1, Reflection 2, Reflection 3, Reflection 4
  - Rows: formatted data, activities as pipe-separated
- Use Blob + download link for file download
- Filename: "moodscaparr-entries-{today's date}.csv"

Create components/history/entry-list.tsx:
- Receives entries array, loading state, empty state
- Renders list of EntryCard components with click handler
- Click → opens EntryDetailDialog
- Loading: skeleton list (5 items)
- Empty: "No entries found" with illustration
- Error: error message with retry

Create app/api/mood-entries/search/route.ts:
- GET /api/mood-entries/search?q=TEXT&category=POSITIVE&page=1&limit=20
- Search across: mood label (derived from moodIndex), reflections (1-4), activities, weather
- Filter by category if provided
- Paginated (offset/limit)
- Auth check, userId filter

Create lib/api/history.ts (client wrappers):
- searchEntries(params): GET /api/mood-entries/search
- fetchAllEntries(): GET /api/mood-entries?limit=1000 (for CSV export)

Create hooks/use-history.ts:
- useSearchEntries(params): TanStack Query hook with search params
- keepPreviousData: true for smooth pagination

Create app/history/page.tsx:
- "use client"
- Layout: page title "Entry History"
- SearchBar + FilterBar in sticky header row
- EntryList with pagination (Load More button at bottom)
- Auth guard (redirect if no session)
- Loading/error/empty states

Create app/history/layout.tsx:
- Page layout with padding, max-width

Create app/history/loading.tsx:
- History skeleton

Create app/history/error.tsx:
- Error boundary
</action>
<done>
Full history page with search, filter, pagination, detail dialog with edit/delete, and CSV export
</done>
</task>

<task type="auto">
<name>Task 2: Calendar heatmap with month navigation and stats</name>
<files>
  app/calendar/page.tsx,
  app/calendar/layout.tsx,
  app/calendar/loading.tsx,
  app/calendar/error.tsx,
  components/calendar/calendar-heatmap.tsx,
  components/calendar/month-stats.tsx,
  components/calendar/color-legend.tsx,
  lib/api/calendar.ts,
  app/api/mood-entries/calendar/route.ts
</files>
<action>
Create app/api/mood-entries/calendar/route.ts:
- GET /api/mood-entries/calendar?month=YYYY-MM
- Return entries for that month + first entry date (for CAL-07)
- Auth check, userId filter
- Response: { entries: MoodEntry[], firstEntryDate: string | null, month: string }

Create lib/api/calendar.ts:
- fetchCalendarEntries(month): GET /api/mood-entries/calendar?month=...

Create components/calendar/calendar-heatmap.tsx:
- Props: entries array, month string, onMonthChange, onDayClick
- Build grid: 7 columns (Sun-Sat), rows for weeks in month
- Cell color based on category + intensity:
  - Empty/no entry: bg-muted/50
  - POSITIVE: scale from --color-mood-positive-light to --color-mood-positive-medium based on intensity
  - NEUTRAL: scale from --color-mood-neutral-light to --color-mood-neutral-medium
  - NEGATIVE: scale from --color-mood-negative-light to --color-mood-negative-medium
  - Today: ring-2 ring-primary
- Month navigation: prev/next arrows, month/year label
- Responsive: full grid on desktop, compact dots on mobile
- Cell size: ~32px on desktop, ~24px on mobile
- Hover: show mood emoji tooltip
- Click: call onDayClick with entry data (or null if no entry)

Create components/calendar/month-stats.tsx:
- Card showing: top mood (emoji + label), entry count, avg intensity, top activity, avg sleep hours
- Uses data from calendar API for the month
- Shadcn/ui Card with small stat items in a row

Create components/calendar/color-legend.tsx:
- Row of 3 color swatches:
  - Green swatch + "Positive"
  - Amber swatch + "Neutral"
  - Red/pink swatch + "Negative"
- Uses category CSS variables for swatch background

Create app/calendar/page.tsx:
- "use client"
- State: currentMonth (Date), selectedEntry
- On mount: determine first entry month from API (CAL-07)
- Fetch calendar entries for current month
- Layout:
  1. Month navigation (prev/next) + current month/year display
  2. MonthStats component
  3. ColorLegend component
  4. CalendarHeatmap component
  5. Selected day detail (inline or dialog)
- Auth guard
- If no entries at all: "Start tracking to see your calendar!" state

Create app/calendar/layout.tsx, loading.tsx, error.tsx
</action>
<done>
Calendar heatmap with navigation, month stats, color legend, day detail on click, starting from first entry date
</done>
</task>
</tasks>

<verification>
- /history shows full entry list, search debounces, category filter works, detail dialog opens on click, edit/delete from dialog, CSV downloads, build passes
- /calendar shows monthly grid with color-coded cells, navigation works, month stats shown, legend displayed, click day shows detail, today highlighted, starts from first entry, build passes
</verification>
