---
phase: 01-foundation-core-diary
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - prisma/seed.ts
  - package.json
  - app/api/seed/route.ts
  - components/ui/error-boundary.tsx
  - components/ui/loading-skeleton.tsx
  - app/error.tsx
  - app/global-error.tsx
  - app/dashboard/loading.tsx
  - app/wizard/loading.tsx
  - app/dashboard/page.tsx
  - app/wizard/page.tsx
  - app/auth/login/page.tsx
  - app/auth/register/page.tsx
autonomous: false
requirements:
  - SEED-01
  - SEED-02
  - VISUAL-01
  - VISUAL-02
  - VISUAL-03
  - VISUAL-04
  - VISUAL-05
must_haves:
  truths:
    - Admin can generate 60 days of sample mood entries via API toggle
    - Admin can clear all sample entries via API toggle
    - All pages have error boundaries that catch runtime errors with recovery UI
    - All data-fetching pages show loading skeletons during fetch
    - All list pages show empty state when no entries exist
    - Every mood selector on every screen uses the same MoodSelector component
    - Every mood color references CSS custom properties (no hardcoded hex/hsl/rgb values)
    - Card rounding tokens are consistently applied across all components
  artifacts:
    - prisma/seed.ts (60-day sample data generator)
    - app/api/seed/route.ts (admin-only seed toggle)
    - components/ui/error-boundary.tsx
    - components/ui/loading-skeleton.tsx
    - app/error.tsx, app/global-error.tsx
    - app/dashboard/loading.tsx, app/wizard/loading.tsx
  key_links:
    - Seed API -> Prisma createMany for generation, deleteMany for clearing
    - Loading.tsx files are auto-picked by Next.js for Suspense fallback
    - ErrorBoundary wraps each page component
    - All mood UI references the shared MoodSelector component (single import path)
    - Design tokens enforced via grep for hardcoded color values

## Phase Goal

**As a** developer and admin, **I want to** generate test data, handle errors gracefully, and verify visual consistency, **so that** the app is testable, resilient, and polished.

<objective>
Add seed data generation for development/testing with admin-only API toggle, implement comprehensive error handling (React error boundaries, Next.js error.tsx, loading skeletons, empty states), and audit all mood elements for visual unification compliance using the shared MoodSelector component and design tokens.

Purpose: Polish the phase with developer tooling, error resilience, empty/loading states, and visual consistency enforcement.
Output: Seedable database with 60 days of realistic entries, graceful error/loading/empty states across all pages, verified visual unification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-core-diary/01-CONTEXT.md
@.planning/phases/01-foundation-core-diary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed data generation with admin toggle</name>
  <files>
    prisma/seed.ts,
    package.json,
    app/api/seed/route.ts
  </files>
  <action>
    Create prisma/seed.ts following RESEARCH.md pattern:
    - Import PrismaClient from generated client
    - Define MOODS constant matching types/mood.ts structure for realistic data
    - Define ACTIVITIES, WEATHERS, REFLECTIONS constant arrays for random selection
    - Check if any entries exist -> skip if true (idempotent)
    - Find first admin user -> skip if none (require admin user to exist)
    - Generate 60 entries (one per day going backwards from today):
      - ~50% positive, ~30% neutral, ~20% negative
      - Random intensity weighted toward 5-7 (Math.floor(Math.random() * 10) + 1, clamp to 5-7 for 60% of entries)
      - 1-3 random activities per entry
      - Random weather
      - Random sleep hours (5-9), energy (3-9), stress (2-8)
      - ~40% chance of reflection text
    - Use createMany for bulk insert
    - Log count of seeded entries

    Update package.json: add "prisma": { "seed": "tsx prisma/seed.ts" }

    Create app/api/seed/route.ts:
    - POST /api/seed: Generate seed data
      - Check auth: require admin role
      - Run seed logic inline (import and call seed function, or use prisma directly)
      - Return { message: "Seeded N entries", count: N }
    - DELETE /api/seed: Clear seed data
      - Check auth: require admin role
      - Delete all entries where userId belongs to first admin user (or provide a mechanism to identify seed entries)
      - Return { message: "Cleared seed data" }
    - GET /api/seed: Check seed status
      - Return { seeded: boolean, count: number }
    - Non-admin users get 403 response
  </action>
  <verify>
    <automated>
      npx prisma db seed runs without error and creates 60 entries,
      Re-running seed skips (outputs skip message),
      POST /api/seed with admin auth -> 200 + creates 60 entries,
      POST /api/seed without auth -> 401,
      POST /api/seed with non-admin auth -> 403,
      DELETE /api/seed with admin auth -> 200 + entries removed,
      npm run build succeeds
    </automated>
  </verify>
  <done>
    Admin can seed 60 days of realistic data and clear it via API, seed script runs via prisma db seed, idempotent
  </done>
</task>

<task type="auto">
  <name>Task 2: Error boundaries, loading skeletons, and empty states</name>
  <files>
    components/ui/error-boundary.tsx,
    components/ui/loading-skeleton.tsx,
    app/error.tsx,
    app/global-error.tsx,
    app/dashboard/loading.tsx,
    app/wizard/loading.tsx,
    app/dashboard/page.tsx,
    app/wizard/page.tsx,
    app/auth/login/page.tsx,
    app/auth/register/page.tsx
  </files>
  <action>
    Create components/ui/error-boundary.tsx:
    - "use client" directive
    - Class component with componentDidCatch and getDerivedStateFromError
    - State: hasError, error
    - Props: fallback?: ReactNode (optional custom fallback), onError?: (error: Error) => void
    - Default fallback: shadcn/ui Alert variant="destructive" with:
      - "Something went wrong" heading
      - Error message
      - "Try Again" button that calls this.setState({ hasError: false })
      - "Go Home" link to /dashboard
    - Logs error to console in dev mode

    Create components/ui/loading-skeleton.tsx:
    - "use client" directive
    - Export Skeleton: shadcn/ui-styled div with animate-pulse and rounded-[--radius-standard], bg-muted
    - Export DashboardSkeleton:
      - Greeting skeleton (Skeleton w-64 h-8)
      - Quick Log skeleton (3 Skeleton buttons in a row)
      - Entry list skeletons (5 items, each with emoji circle + text lines)
    - Export WizardSkeleton:
      - Step indicator skeleton (3 dots in a row)
      - Mood grid placeholder (3 columns with 7 skeleton circles each)
      - Button skeleton at bottom

    Create app/error.tsx:
    - "use client" directive
    - Renders ErrorBoundary with default fallback
    - Catches errors from page.tsx and all child client components
    - export default function Error({ error, reset })

    Create app/global-error.tsx:
    - "use client" directive
    - Minimal HTML fallback (no client-side framework needed since root layout may have failed)
    - Shows "Critical error" message with reset button
    - Inline styles only (CSS may not load if root layout failed)

    Create app/dashboard/loading.tsx:
    - Renders DashboardSkeleton
    - Next.js automatically shows this during page load (Suspense boundary)

    Create app/wizard/loading.tsx:
    - Renders WizardSkeleton

    Update app/dashboard/page.tsx:
    - Wrap page content in ErrorBoundary
    - Empty state: when entries array is empty, show:
      - Illustration/icon (Feather or Heart from lucide-react)
      - "No entries yet" heading
      - "Start tracking your mood! Your first entry is just a tap away."
      - "Log Your First Mood" button -> navigates to /wizard
      - Quick Log still visible even in empty state (so user can quick-log without navigating)
    - Error state: when useMoodEntries returns error, show:
      - "Couldn't load entries" with error message
      - "Try Again" button that refetches

    Update app/wizard/page.tsx:
    - Wrap in ErrorBoundary
    - Show loading skeleton while fetching entry for edit mode
    - Show error state if entry not found (invalid ?id param) with "Entry not found" message and link to /dashboard

    Update app/auth/login/page.tsx:
    - Wrap in ErrorBoundary

    Update app/auth/register/page.tsx:
    - Wrap in ErrorBoundary
  </action>
  <verify>
    <automated>
      npm run build succeeds,
      Visit /dashboard -> loading skeleton appears briefly then content loads,
      Visit /dashboard with no entries -> empty state shows with "Log Your First Mood" button,
      Visit /wizard -> loading skeleton appears,
      Visit /wizard?id=invalid -> error state "Entry not found",
      Simulate component error -> ErrorBoundary fallback renders with "Try Again",
      Click "Try Again" -> error clears, component re-renders
    </automated>
  </verify>
  <done>
    Error boundaries catch and recover from runtime errors, loading skeletons show during data fetch, empty states guide new users, auth pages are wrapped in error boundaries
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual unification audit</name>
  <what-built>MoodSelector, design tokens, all mood UI across wizard, Quick Log, and entry cards</what-built>
  <how-to-verify>
    1. Navigate to /wizard — verify the mood emoji grid:
       - 3-column layout (Positive | Neutral | Negative)
       - Each column has category header with color indicator
       - Emoji size is consistent (2rem / 32px)
       - Label text is consistent (0.875rem)
       - Button padding and rounding is consistent (rounded-[--radius-standard])
       - Hover animation (scale 1.05) works on all mood buttons

    2. Navigate to /dashboard — verify Quick Log:
       - Category buttons match wizard's category colors
       - Expanded mood grid matches wizard's mood layout exactly (same component)
       - Same emoji size, text size, spacing, hover animations

    3. Verify entry cards:
       - Card rounding uses --radius-moderate (16px)
       - Category color strip matches --color-mood-*-medium

    4. Verify CSS variables (open devtools inspector):
       - --color-mood-positive-medium is oklch(0.62 0.19 145) in light mode
       - --color-mood-positive-medium is oklch(0.52 0.2 145) in dark mode
       - --radius-pill: 24px, --radius-moderate: 16px, --radius-standard: 8px, --radius-slight: 4px

    5. Toggle dark mode — verify all mood colors adjust:
       - Positive green shifts to dark variant
       - Neutral amber shifts to dark variant
       - Negative red shifts to dark variant

    6. Code audit: grep for hardcoded color values in component files:
       - Search for '#' (hex colors) in all .tsx files under components/, app/
       - Search for 'rgb' and 'hsl' in same files
       - All mood-related colors should reference Tailwind classes (bg-mood-*, text-mood-*, border-mood-*)
  </how-to-verify>
  <resume-signal>Approved (visual unification confirmed) OR describe discrepancies found and I will fix them</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server | Seed toggle requests |
| Server -> Database | Bulk create/delete operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Denial of Service | Seed endpoint | low | mitigate | Seed API restricted to admin users; not accessible to anonymous or non-admin requests |
| T-01-12 | Data Loss | Clear seed data | low | accept | Clear only deletes entries tied to admin user; admin-only access; clear operation logged |
</threat_model>

<verification>
- Seed script creates 60 entries across 60 days with realistic distribution
- Seed API returns proper responses for admin, non-admin, and unauthenticated users
- Error boundaries catch thrown errors across all pages
- Loading skeletons display during initial data fetches
- Empty states guide users with CTA to log first mood
- No hardcoded color values for mood categories in any component file
- All mood selectors reference the same MoodSelector import
- All card rounding uses CSS variable tokens
- Build passes with zero TypeScript errors
</verification>

<success_criteria>
1. Running seed creates 60 sample entries across 60 consecutive days
2. Clearing seed removes all sample entries
3. Error boundaries catch render errors with "Try Again" recovery
4. Loading skeletons render during data fetch for dashboard and wizard
5. Empty states show when user has no entries (with Quick Log still available)
6. All mood selectors across wizard, Quick Log, and entry views use the shared MoodSelector component
7. Zero hardcoded hex/hsl/rgb color values for mood categories in any component
8. Card rounding tokens are consistently applied (--radius-moderate for entry cards, --radius-standard for stat cards)
</success_criteria>

<output>
Create .planning/phases/01-foundation-core-diary/01-03-SUMMARY.md when done
</output>
