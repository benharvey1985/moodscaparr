---
phase: 01-foundation-core-diary
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - types/mood.ts
  - lib/api/mood-entries.ts
  - hooks/use-mood-entry.ts
  - app/api/mood-entries/route.ts
  - app/api/mood-entries/[id]/route.ts
  - components/ui/mood-selector.tsx
  - components/wizard/wizard-provider.tsx
  - components/wizard/step-mood-picker.tsx
  - components/wizard/step-context.tsx
  - components/wizard/step-reflection.tsx
  - components/wizard/wizard-page.tsx
  - components/quick-log.tsx
  - components/entry-card.tsx
  - components/delete-dialog.tsx
  - components/header.tsx
  - app/wizard/page.tsx
  - app/wizard/layout.tsx
  - app/dashboard/page.tsx
autonomous: true
requirements:
  - MOOD-01
  - MOOD-02
  - MOOD-03
  - MOOD-04
  - MOOD-05
  - MOOD-06
  - MOOD-07
  - MOOD-08
  - MOOD-09
  - MOOD-10
  - QUICK-01
  - QUICK-02
  - EDIT-01
  - EDIT-02
  - EDIT-03
must_haves:
  truths:
    - User can log a mood via a 3-step wizard picking from 19 moods across 3 categories
    - User can set mood intensity (1-10) and date during wizard
    - User can add context: activities (multi-select), weather, sleep, energy, stress
    - User can optionally respond to 4 reflection prompts
    - Wizard uses progressive disclosure (context steps collapsed behind "Add more detail")
    - User can quick-log a mood in <=3 taps from dashboard with today's date and intensity=5
    - User can edit existing entries via pre-filled wizard
    - User can delete entries with AlertDialog confirmation (permanent)
    - All mood selectors use the same MoodSelector component (consistent emoji size, grid, colors)
  artifacts:
    - types/mood.ts (19 moods, categories, activities, weathers, types)
    - app/api/mood-entries/route.ts (GET/POST)
    - app/api/mood-entries/[id]/route.ts (PUT/DELETE)
    - hooks/use-mood-entry.ts (TanStack Query hooks with optimistic updates)
    - components/ui/mood-selector.tsx (shared, single source of truth)
    - components/wizard/ (full wizard suite: provider + 3 steps + page)
    - components/quick-log.tsx
    - components/entry-card.tsx and components/delete-dialog.tsx
    - app/wizard/page.tsx (create + edit mode)
    - app/dashboard/page.tsx (enhanced with Quick Log and entry list)
  key_links:
    - MoodSelector used by both wizard and Quick Log (VISUAL-02 compliance)
    - Wizard submits -> API route -> Prisma -> DB with TanStack Query cache update
    - Quick Log saves -> optimistic cache update -> instant UI feedback
    - Edit navigates to /wizard?id=X -> pre-fills form -> update mutation
    - Delete shows AlertDialog -> confirm -> optimistic removal -> cache invalidation
    - All mood colors reference CSS variables from globals.css (VISUAL-05 compliance)

## Phase Goal

**As a** user, **I want to** log my daily mood quickly or with full detail, and manage past entries, **so that** I can track my emotional wellbeing consistently.

<objective>
Build the core diary functionality: shared MoodSelector component (single source of truth for all mood UI), 3-step wizard with progressive disclosure, Quick Log from dashboard, entry cards with edit/delete, and TanStack Query hooks with optimistic updates for instant UI feedback.

Purpose: Deliver the primary user action (logging moods) and basic entry management.
Output: Working wizard, quick log, and edit/delete with instant UI feedback via optimistic updates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-core-diary/01-CONTEXT.md
@.planning/phases/01-foundation-core-diary/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mood types, API routes, and TanStack Query hooks with optimistic updates</name>
  <files>
    types/mood.ts,
    lib/api/mood-entries.ts,
    hooks/use-mood-entry.ts,
    app/api/mood-entries/route.ts,
    app/api/mood-entries/[id]/route.ts
  </files>
  <action>
    Create types/mood.ts:
    - Export const MOODS with 3 categories, 19 total mood entries, each with { emoji: string, label: string }:
      POSITIVE: [
        { emoji: "😊", label: "Ecstatic" }, { emoji: "😄", label: "Happy" },
        { emoji: "😌", label: "Content" }, { emoji: "🔆", label: "Hopeful" },
        { emoji: "🙏", label: "Grateful" }, { emoji: "🎉", label: "Excited" },
        { emoji: "💕", label: "Loved" }
      ],
      NEUTRAL: [
        { emoji: "🧘", label: "Calm" }, { emoji: "⚖️", label: "Balanced" },
        { emoji: "😐", label: "Indifferent" }, { emoji: "🤔", label: "Thoughtful" },
        { emoji: "👀", label: "Curious" }, { emoji: "😴", label: "Tired" }
      ],
      NEGATIVE: [
        { emoji: "😢", label: "Sad" }, { emoji: "😰", label: "Anxious" },
        { emoji: "😠", label: "Angry" }, { emoji: "😤", label: "Frustrated" },
        { emoji: "🥺", label: "Lonely" }, { emoji: "😩", label: "Stressed" }
      ]
    - Export type MoodCategory = "POSITIVE" | "NEUTRAL" | "NEGATIVE"
    - Export interface MoodEntry matching Prisma model shape
    - Export interface CreateMoodEntryInput (omit id, userId, createdAt, updatedAt)
    - Export interface UpdateMoodEntryInput (Partial of CreateMoodEntryInput)
    - Export ACTIVITIES: ["Work", "Exercise", "Social", "Hobbies", "Relaxation", "Health", "Family", "Food", "Travel", "Reading", "Gaming", "Music", "Nature", "Shopping", "Learning"]
    - Export WEATHERS: ["Sunny", "Cloudy", "Rainy", "Stormy", "Snowy", "Windy", "Foggy"]
    - Export SLEEP_QUALITIES: ["Poor", "Fair", "Good", "Great"]

    Create app/api/mood-entries/route.ts:
    - GET: fetch entries for authenticated user (from session), ordered by date DESC, limit 50
    - POST: create entry, validate body with Zod (category enum, moodIndex 0-18, intensity 1-10, date ISO string, all others optional)
    - Auth check: return 401 if no session
    - userId from session, never from request body (T-01-07 mitigation)

    Create app/api/mood-entries/[id]/route.ts:
    - PUT: update entry by ID, verify ownership (entry.userId === session.user.id), return 404 if not found or not owned
    - DELETE: delete entry by ID, verify ownership, return { success: true }
    - Both return 401 if unauthenticated

    Create lib/api/mood-entries.ts (client-side fetch wrappers):
    - fetchMoodEntries(): GET /api/mood-entries with credentials: "include"
    - createMoodEntry(data): POST /api/mood-entries with JSON body
    - updateMoodEntry(id, data): PUT /api/mood-entries/[id]
    - deleteMoodEntry(id): DELETE /api/mood-entries/[id]
    - All throw on non-ok response

    Create hooks/use-mood-entry.ts:
    - export const moodKeys = { all: ["mood-entries"] as const, byDate: (date: string) => ["mood-entries", "date", date] as const, byRange: (from, to) => ["mood-entries", "range", from, to] as const, latest: () => ["mood-entries", "latest"] as const }
    - export function useMoodEntries(): useQuery with moodKeys.all, queryFn fetchMoodEntries
    - export function useCreateMoodEntry(): useMutation with:
      - onMutate: cancel queries, snapshot cache, add optimistic entry with temp id, return snapshot
      - onError: rollback to snapshot
      - onSettled: invalidate mood-entries queries
    - export function useUpdateMoodEntry(): useMutation with:
      - onMutate: cancel, snapshot, update in-place in cache, rollback on error
    - export function useDeleteMoodEntry(): useMutation with:
      - onMutate: cancel, snapshot, filter out from cache, rollback on error
    - Follow RESEARCH.md cache manipulation patterns exactly
  </action>
  <verify>
    <automated>
      npm run build succeeds,
      POST /api/mood-entries with valid body -> 201 + created entry returned,
      POST /api/mood-entries without auth -> 401,
      GET /api/mood-entries with auth -> array of entries,
      PUT /api/mood-entries/:id with ownership -> updated entry,
      PUT /api/mood-entries/:id without ownership -> 404,
      DELETE /api/mood-entries/:id with ownership -> { success: true },
      DELETE /api/mood-entries/:id without ownership -> 404
    </automated>
  </verify>
  <done>
    Mood entry CRUD API works with server-side auth and ownership checks; TanStack Query hooks provide optimistic updates
  </done>
</task>

<task type="auto">
  <name>Task 2: Shared MoodSelector and 3-step wizard with progressive disclosure</name>
  <files>
    components/ui/mood-selector.tsx,
    components/wizard/wizard-provider.tsx,
    components/wizard/step-mood-picker.tsx,
    components/wizard/step-context.tsx,
    components/wizard/step-reflection.tsx,
    components/wizard/wizard-page.tsx,
    app/wizard/page.tsx,
    app/wizard/layout.tsx
  </files>
  <action>
    Create components/ui/mood-selector.tsx (shared single source of truth per VISUAL-02/VISUAL-03):
    - "use client" directive
    - Props: selectedCategory: MoodCategory | null, selectedMoodIndex: number | null, onMoodSelect: (category: MoodCategory, index: number) => void, intensity: number, onIntensityChange: (value: number) => void, date: string, onDateChange: (date: string) => void
    - 3-column grid: each column has category label (Positive/Neutral/Negative) with color indicator, then 6-7 mood emoji+label buttons
    - Category column header uses the corresponding --color-mood-*-medium as background/border
    - Each mood button: emoji (2rem), label text (0.875rem), consistent padding and rounded-[--radius-standard]
    - Selected mood button gets highlighted bg using --color-mood-*-medium, non-selected gets --color-mood-*-light
    - Hover: scale(1.05) animation, cursor pointer
    - Intensity slider (1-10) with labels at each end (1=Mild, 10=Intense), styled with mood color
    - Date input (type="date") below the slider, defaulting to today
    - Responsive: 3 columns on desktop, single column on mobile
    - NO hardcoded colors — all via Tailwind utility classes from design tokens

    Create components/wizard/wizard-provider.tsx per RESEARCH.md pattern:
    - React Context with: step (0|1|2), formData (full schema), updateFormData, setStep, resetForm
    - Zod schemas per step: step1Schema (category, moodIndex, intensity, date), step2Schema (activities, weather, sleepHours, sleepQuality, energyLevel, stressLevel — all optional), step3Schema (reflection1-4 — all optional, max 500)
    - fullSchema = step1Schema.merge(step2Schema).merge(step3Schema)
    - localStorage draft buffer: useEffect watches form changes and saves to "mood-draft" key; on mount, restores from localStorage
    - clearDraft() function called after successful save

    Create components/wizard/step-mood-picker.tsx:
    - Renders MoodSelector with wizard context values
    - Two buttons: "Save" (variant="ghost", saves with current data — triggers mutation directly) and "Add More Detail" (advances to step 2)
    - "Save" button shows if user wants to skip context/reflection

    Create components/wizard/step-context.tsx (MOOD-04 to MOOD-08):
    - All fields optional per MOOD-10 (progressive disclosure — this step shown when user clicks "Add More Detail")
    - Activities: multi-select chip grid (click to toggle, selected chips have mood category color)
    - Weather: shadcn/ui Select component
    - Sleep hours: number input (0-24) + Sleep quality: Select (Poor/Fair/Good/Great)
    - Energy level: slider (1-10) with labels
    - Stress level: slider (1-10) with labels
    - "Back" and "Next" buttons

    Create components/wizard/step-reflection.tsx:
    - 4 textareas with character counter (max 500 each) per MOOD-09:
      1. "What went well?" (reflection1)
      2. "What was challenging?" (reflection2)
      3. "What are you grateful for?" (reflection3)
      4. "What's on your mind?" (reflection4)
    - Each textarea is optional, shadcn/ui Textarea component
    - Character count: "{count}/500" below each textarea
    - "Back" and "Save Entry" buttons

    Create components/wizard/wizard-page.tsx:
    - Orchestrates the 3 steps using WizardProvider
    - Step indicator at top: horizontal progress bar with 3 steps labeled "Mood", "Context", "Reflect"
    - Current step highlighted with mood color, completed steps green, future steps gray
    - Renders the appropriate step component based on current step
    - Final submit: calls useCreateMoodEntry mutation with full form data
    - On mutation success: clear draft, show success toast, redirect to /dashboard
    - On mutation error: show error message

    Create app/wizard/page.tsx:
    - Check for ?id=ENTRY_ID search param (edit mode)
    - If id present: fetch entry data, pre-fill WizardProvider via resetForm
    - Title: "Edit Entry" vs "New Entry"
    - Auth guard: redirect to /auth/login if no session
    - HydrationBoundary for prefetched entry data

    Create app/wizard/layout.tsx:
    - Centered layout, max-width 2xl, padding
  </action>
  <verify>
    <automated>
      npm run build succeeds,
      Visit /wizard -> wizard renders with step indicator at step 1,
      Step 1: 19 moods in 3 columns with emoji and labels,
      Clicking a mood highlights it with category color,
      Intensity slider works,
      Date picker defaults to today,
      "Add More Detail" advances to step 2,
      Step 2: activity chips, weather select, sleep inputs, energy/stress sliders render,
      "Next" advances to step 3,
      Step 3: 4 reflection textareas with character counters render,
      "Save Entry" creates entry and redirects to dashboard,
      Entry appears in database with all values,
      localStorage draft restores on page refresh
    </automated>
  </verify>
  <done>
    Full 3-step wizard works end-to-end with all 19 moods, intensity, date, context fields, reflections, progressive disclosure, and localStorage draft backup
  </done>
</task>

<task type="auto">
  <name>Task 3: Quick Log, dashboard, edit/delete</name>
  <files>
    components/quick-log.tsx,
    components/entry-card.tsx,
    components/delete-dialog.tsx,
    components/header.tsx,
    app/dashboard/page.tsx,
    app/wizard/page.tsx,
    app/page.tsx
  </files>
  <action>
    Create components/quick-log.tsx:
    - "use client" directive
    - Two states: collapsed (shows 3 category buttons) and expanded (shows mood grid for selected category)
    - 3 category buttons in a row: "😊 Positive" (green), "🧘 Neutral" (amber), "😢 Negative" (red)
    - Each button uses its category's CSS variable for bg/border on hover
    - Click category -> expands inline mood grid with that category's 6-7 moods
    - Click mood -> instantly calls useCreateMoodEntry with category, moodIndex, date=today, intensity=5 (QUICK-02 defaults)
    - Uses mutation.variables pattern (simpler) per RESEARCH.md recommendation
    - Shows brief loading state during save
    - On success: toast "Mood logged!" and invalidate queries
    - <=3 taps: tap category -> tap mood -> auto-saves (QUICK-01)

    Create components/entry-card.tsx:
    - Displays: mood emoji (2rem), mood label, formatted date (date-fns), category color strip on left border
    - Background uses card rounding token (--radius-moderate)
    - "Edit" button (pencil icon) and "Delete" button (trash icon) in top-right
    - Category color indicator via CSS variable

    Create components/delete-dialog.tsx:
    - shadcn/ui AlertDialog
    - Title: "Delete Entry"
    - Description: "Are you sure? This will permanently delete this entry."
    - Cancel button and destructive Delete button
    - Uses useDeleteMoodEntry mutation
    - On success: toast "Entry deleted" + query invalidation

    Create components/header.tsx (enhanced from 01-01):
    - App logo/name "Moodscaparr"
    - Navigation links: Dashboard (home), New Entry (/wizard)
    - ThemeToggle component
    - User avatar circle with first initial, dropdown with name and Logout
    - Logout calls authClient.signOut() and redirects to /auth/login

    Update app/dashboard/page.tsx (enhance the 01-01 placeholder):
    - "use client" directive for interactivity
    - Fetch session and mood entries
    - Time-aware greeting: "Good morning/afternoon/evening, {user.name}" using date-fns
    - Today's status: "You've logged today! 🎉" or "No entry yet today"
    - Quick Log section (renders QuickLog component)
    - Recent entries list: last 5 entries as EntryCards
    - If no entries: empty state "Start tracking your mood!" with link to /wizard
    - Loading state: DashboardSkeleton while fetching
    - Error state: error message with retry button

    Update app/wizard/page.tsx for edit mode (MOOD-03, EDIT-01):
    - Check for ?id= search param
    - If editing: fetch entry, pre-fill wizard using resetForm
    - Submit calls useUpdateMoodEntry instead of useCreateMoodEntry
    - Title changes to "Edit Entry"
    - On success: redirect to /dashboard with toast "Entry updated"

    Update app/page.tsx: redirect authenticated users to /dashboard, unauthenticated to /auth/login
  </action>
  <verify>
    <automated>
      npm run build succeeds,
      Visit /dashboard -> greeting shows with user's name,
      Quick Log renders 3 category buttons,
      Click category -> mood grid expands inline,
      Click mood -> entry saves instantly (appears in recent list),
      Edit icon on entry -> navigates to /wizard?id=X with pre-filled form,
      Delete icon -> AlertDialog opens,
      Confirm delete -> entry removed from UI,
      No entries -> empty state with link to wizard
    </automated>
  </verify>
  <done>
    Quick Log saves in <=3 taps, Edit opens pre-filled wizard, Delete permanently removes with confirmation,
    Dashboard shows greeting, Quick Log, and recent entries
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server | Mood entry data crosses here |
| Server -> Database | All CRUD operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | Mood entry API | high | mitigate | Zod validation on all inputs; userId from session (never request body); ownership checks on update/delete |
| T-01-08 | Information Disclosure | Mood entry list | medium | mitigate | Server filters entries by session userId; API never returns other users' data |
| T-01-09 | Elevation of Privilege | Edit/Delete | high | mitigate | Ownership verification on every mutation: entry.userId must match session.user.id |
| T-01-10 | Data Loss | Browser close during wizard | low | mitigate | localStorage draft buffer saves progress on each field change; restored on mount |
</threat_model>

<verification>
- Wizard creates entries with all fields in database
- Quick Log saves entry with intensity=5 and today's date
- Edit mode pre-fills wizard with existing entry data
- Delete permanently removes entry and updates UI optimistically
- All mood selectors use the same MoodSelector component
- All mood colors reference CSS variables via Tailwind utilities
- Optimistic updates roll back on API error
- Progressive disclosure: step 2 and 3 visible only after clicking "Add more detail"
</verification>

<success_criteria>
1. User completes full wizard -> entry in DB with all context and reflection fields
2. User quick-logs from dashboard -> entry in DB with default intensity=5, today's date, no context
3. User edits entry -> pre-filled wizard -> changes saved in DB
4. User deletes entry -> AlertDialog confirmation -> removed from DB and UI instantly
5. All mood selectors identical (same MoodSelector component, same emoji size, same grid)
6. Progressive disclosure works: step 1 visible, steps 2-3 behind "Add more detail" button
7. Dashboard shows time-aware greeting, today's status, Quick Log, and recent entries
</success_criteria>

<output>
Create .planning/phases/01-foundation-core-diary/01-02-SUMMARY.md when done
</output>
